name: delete-old-packages
on:
  workflow_dispatch:

jobs:
  delete-old-packages:
    runs-on: ubuntu-latest
    
    steps:
      - name: Delete package
        uses: actions/delete-package-versions@0739cce1e8d24224c6a7e0f4cdbb444db3a37f6f
        with: 
            owner: NamrataJha
            repo: only-pkg
            package-name: only-pkg
            num-old-versions-to-delete: 10

 


          
