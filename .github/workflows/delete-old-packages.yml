name: delete-old-packages
on:
  workflow_dispatch:

jobs:
  delete-old-packages:
    runs-on: ubuntu-latest
    
    steps:
      - name: Delete package
        uses: actions/delete-package-versions@1369f0ca05c355c68d7a5dc5e33bb54b69ae4a10
        with: 
            owner: NamrataJha
            repo: only-pkg
            package-name: only-pkg
            num-old-versions-to-delete: 10

 


          
