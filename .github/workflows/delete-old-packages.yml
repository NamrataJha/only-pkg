name: delete-old-packages
on:
  workflow_dispatch:

jobs:
  delete-old-packages:
    runs-on: ubuntu-latest
    
    steps:
      - name: Delete package
        uses: actions/delete-package-versions@c8087bcc700995f151a1ff38b699fe67538e0f67
        with: 
            owner: NamrataJha
            repo: only-pkg
            package-name: only-pkg
            num-old-versions-to-delete: 150

 


          
